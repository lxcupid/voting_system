import 'package:flutter/material.dart';

class VotinsystemVotingLine extends StatelessWidget {
  const VotinsystemVotingLine({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('VOTING LINE'),
      ),
    );
  }
}
